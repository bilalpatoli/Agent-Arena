# Copy to .env.local and fill in when you have a key.
# Leave unset → the arena runs on the deterministic mock runner (demo-safe).
# Set it → the HybridRunner uses live Gemini and falls back to mock on any error.
GEMINI_API_KEY=
